# Database Configuration
DATABASE_URL=postgresql+asyncpg://user:secure_password@db:5432/craneshell

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-this-in-production-min-32-chars
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5500,*

# Server Configuration
DEBUG=True
PORT=8000

# Database Admin (используется в docker-compose)
POSTGRES_USER=user
POSTGRES_PASSWORD=secure_password_change_me
POSTGRES_DB=craneshell
